let number = 10;
console.log(number);
// Number => literally the value

/* Strings => strings are datatypes used to represent text
Wrap them in single or double quotes
*/

let stringOne = "double quotes";
let stringTwo = 'single quotes';

console.log(stringOne, stringTwo);

/** Booleans => true or false */
 let on = true;
 let off = false;
 console.log(on,off);

//  Null => Null is an empty value (container with nothing in it ) null is intentional 
let empty = null;
console.log(empty);

//  Undefined => no value is assigned and does not act as an empty container ( has not been set or not been created yet.)
let undef = undefined
console.log(undef);
// correct way for undefined JS 
let correct;
console.log(correct);

// Objects are used to store many values instead of one single value { denoted by {}key value paairs}

let frodo = {
    race: 'hobbit',
    rings: 1,
    cloak: true
}
console.log(frodo);
console.log(typeof frodo); 

// Arrays are containers that hold lists of items.
// Denoted by square brackets[]

let list = ['item1','item2','item3'];
console.log(list);

let burritos = ['large', 4, true];
console.log(burritos);
console.log(typeof burritos);

// Data Types Literals
// A literal represents a fixed value that we as developers insert into the code.
// includes strings, numbers, booleans, objects, arrays.

// string literals
let car = "Ford";

//  Number Literals
Let December = 12;

// Boolean Literals
Let tired = true;

// Object Literals 
let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
    d: 'chili',
    e: 'cereal'
};

// Array literal
let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];


